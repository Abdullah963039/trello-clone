interface BoardIdPageProps {
  params: {
    boardId: string;
  };
}
const BoardIdPage = (props: BoardIdPageProps) => {
  return <div className="py-24">BoardIdPage</div>;
};
export default BoardIdPage;
